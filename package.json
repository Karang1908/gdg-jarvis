{
  "name": "gdg-jarvis",
  "version": "1.0.0",
  "description": "Local-network room control plane for a GDG IoT demonstration",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node core/server.js",
    "test": "node core/test/wire.test.js",
    "health": "bash scripts/health-check.sh"
  },
  "_dependencies": "There are none, on purpose. During the demo the Kali machine's Wi-Fi is the access point, so it has no internet — a Core that needs `npm install` is a Core that can fail to start at the venue. See docs/DEVIATIONS.md D1.",
  "dependencies": {}
}
