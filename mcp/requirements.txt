# The MCP SDK is the only dependency. Everything else is the standard library on purpose:
# this runs on the Presenter Mac beside the AI client, and the fewer moving parts on the
# one machine that also drives the projector, the better.
#
#   python3 -m venv .venv && .venv/bin/pip install -r mcp/requirements.txt
#
# Install this days before the demo. The Presenter Mac reaches the internet only through a
# tethered iPhone, and pip over cellular in a venue is not a thing to plan around.
mcp>=1.2.0
